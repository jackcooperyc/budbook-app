/**
 * In-memory HTML fixture strings (also available as `.html` files beside this module).
 * Prefer `parseFixtureGeneric()` / `loadFixtureHtml()` from `./samples` for verification.
 */
export {
  genericCoaHtml as genericCoaHtmlFixture,
  fidelityCoaHtml as fidelityCoaHtmlFixture,
  insufficientHtml as insufficientCoaHtmlFixture,
} from './samples';
